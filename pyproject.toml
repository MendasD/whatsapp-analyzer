[project]
name = "whatsapp-analyser"
version = "0.1.0"
description = "WhatsApp package to analyse discussions from a WhatsApp group"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langdetect>=1.0.9",
    "pandas>=3.0.1",
    "pytest>=9.0.2",
    "rich>=14.3.3",
]
